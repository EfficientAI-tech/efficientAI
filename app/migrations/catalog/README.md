# Catalog-only migrations (headers, registry, integrations).
# Postgres database name is typically ``efficientai_catalog`` (see config.yml.example).
# Set ``MIGRATION_SCOPE = "catalog"`` on new migration modules in this directory
# once the hard split is enforced (Phase 8).
