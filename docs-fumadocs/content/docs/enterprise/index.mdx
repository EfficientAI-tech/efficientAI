---
title: Enterprise
description: Production-scale voice agent evaluation with team IAM, extended analytics, and operational controls beyond open-source limits.
---

# Enterprise

<EnterpriseQuickstart />

## Who is Enterprise for?

Enterprise is for teams running voice agent evaluation in production who need multi-member IAM, extended analytics, gateway routing, and operational controls beyond the open-source limits.

## License model (BSL)

EfficientAI open source is released under the **Business Source License (BSL)**:

- Core evaluation workflows remain free to self-host under BSL.
- Production-scale capabilities require an **Enterprise license key** (JWT).
- After the change date in the repository `LICENSE` file, covered code converts to Apache 2.0.

Enterprise contracts unlock gated product features for your organization. When `org_id` is omitted in the license payload, the same features apply deployment-wide (typical for self-hosted installs).

## Open source vs Enterprise

<EnterpriseComparison />

## Enterprise capabilities

Items marked **Licensed** in the comparison table require an Enterprise entitlement. The following areas are included in that tier:

<EnterpriseLicensedCapabilities />

Enterprise authentication (OIDC, SAML, SCIM, multi-factor enforcement, and audit export) is configured separately; see the [Authentication guide](/docs/getting-started/authentication/).

## Included in open source

The comparison table above is the source of truth. In short, open source includes:

- Voice bundles and integrations (BYOK and platform providers)
- Agents, personas, and scenarios (within agent limits)
- Evaluators, suites, and evaluation results
- Metrics and categorization labels (within metric limits)
- Prompt partials, GEPA / prompt optimization, and agent playground
- Traces, observability, and judge alignment
- API key and local password authentication

## Set up a license

Provide the Enterprise license key in environment variables or config:

```bash title=".env"
EFFICIENTAI_LICENSE=eyJhbGciOi...
```

```yaml title="config.yml"
license:
  key: "eyJhbGciOi..."
```

Restart the EfficientAI backend after updating the license.

**Scope**

- `org_id` omitted — features apply to the entire deployment.
- `org_id` set — features apply only to that organization.

Verify entitlements with `GET /api/v1/license-info` after restart.

## FAQ

<EnterpriseFaq />

<EnterpriseContactCta />
